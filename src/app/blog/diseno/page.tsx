"use client"

export default function Diseño() {

    return (
        <p>Diseño</p>
    )
}

/*
    const ssc = (n)=> {
        return (
            new Promise((resolve) => {
                setTimeout(() => {resolve(n)}, 5000);
            })
        )
    }
    async function  segundos () {
    
        const response = await ssc(' 5 segundos')
        
        console.log(response)
    }
    segundos()
*/